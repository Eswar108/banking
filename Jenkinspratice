pipeline
{
    agent any
    stages
    {
        stage ("To Launch instance")
        {
            steps
            {
                sh "cd terraform"
                sh "export AWS_ACCESS_KEY_ID=AKIATIPRQMDOSNDV5L2Z"
                sh "export AWS_SECRET_ACCESS_KEY=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"
                sh "export AWS_DEFAULT_REGION=us-west-1"
                sh "terraform init"
                sh "terraform apply --auto-approve"
            }
        }
    }
}
